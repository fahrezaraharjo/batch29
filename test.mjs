import readline from "readline"
import fs from "fs"
  input: process.stdin,
  output: process.stdout,
  prompt: 'tebakan :'
});

console.log("selamat datang di game tebak-tebakan jawab pertanyaan ini dengan benar ya :)")
rl.prompt();
if()
rl.on('line', (line) => {
  
  }
  rl.prompt();
}).on('close', () => {
  console.log('Have a great day!');
  process.exit(0);
});